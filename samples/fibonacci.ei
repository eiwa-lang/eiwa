fun fibonacci(n: Int): Int {
    var a = 0
    var b = 1
    var count = 0
    
    while (count < n) {
        var temp = a
        a = b
        b = temp + b
        count = count + 1
    }
    
    return a
}

