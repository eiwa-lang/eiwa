import { assert } from "std.core"

type Point(var x: Int, var y: Int)

test "llvm backend type creation and member access and mutation" {
    val p = Point(10, 20)
    assert(p.x == 10)
    assert(p.y == 20)
    p.x = 99
    assert(p.x == 99)
}
