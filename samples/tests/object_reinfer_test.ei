import { assert } from "std.core"

object Greeter {
    fun hello(name: String): String = "hi " + name
}

test "object method call survives transform re-inference" {
    val msg = Greeter.hello("eiwa")
    assert(msg == "hi eiwa")
    val t = task { 42 }
    assert(t.await() == 42)
}
