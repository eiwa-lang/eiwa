import { CircularGroup } from "circular_group"
import { print } from "std.core"

type CircularUser(val name: String, val group: CircularGroup) {
    fun show() {
        print("User: " + name + " in group " + group.name)
    }
}
