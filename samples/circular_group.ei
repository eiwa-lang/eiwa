import { CircularUser } from "circular_user"
import { print } from "std.core"

type CircularGroup(val name: String, var owner: CircularUser?) {
    fun show() {
        val ownerName = owner?.name ?: "No Owner"
        print("Group: " + name + ", Owner: " + ownerName)
    }
}
